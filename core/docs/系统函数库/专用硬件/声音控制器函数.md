# 音量控制器函数 (help)

| **_ 音量控制器函数 CREATOR _** | [后退](模拟卡控制函数.md "模拟卡控制函数")[前进](墙上面板控制函数.md "墙上面板控制函数") |     |     |
| ----------------------- | ------------------------------------------------------ | --- | --- |
|                         |                                                        |     |     |

_导航: 帮助 > [控制设备函数](控制设备函数.md "MINICC设备控制函数") > _
* * *
| ###  ON_VOL | OFF_VOL | SET_VOLTOTAL | SET_VOLHIGHT | SET_VOLLOW |
| --- | --- | --- | --- | --- |
| --- | --- | --- | --- | --- |
| Z_SET_VOLHIGH | Z_SET_VOLLOW | Z_SET_VOLTOTAL | Z_SET_VOLQUIET |

* * *
ON_VOL
```text
  void ON_VOL(String dev,int channel)
```

```text

```

功能：打开音量
**Parameters:**
`dev` \- :音量设备
`channel` \- :设备通道号
示例：
vol_N = N:9:VOL; //定义CRNET设备号为9的音量设备vol_N
ON_VOL(vol_N,1); //打开vol_N的第一路
* * *
# ## OFF_VOL
```text
  void OFF_VOL(String dev,int channel)
```

```text

```

功能：关闭音量
**Parameters:**
`dev` \- :音量设备
`channel` \- :设备通道号
示例：
vol_N = N:9:VOL; //定义CRNET设备号为9的音量设备vol_N
ON_VOL(vol_N,1); //关闭vol_N的第一路
* * *
# ## SET_VOLTOTOL
```text
  void SET_VOLTOTOL(String dev,int channel,int val)
```

```text

```

功能：控制总音量部分
**Parameters:**
`dev` \- :音量设备
`channel` \- :设备通道号
`val` \- ：模拟量（注：根据具体外接设备取值。）
示例：
vol_N = N:9:VOL; //定义CRNET设备号为9的音量设备vol_N
SET_VOLTOTOL(vol_N,1,600);//设置vol_N设备第一路总音量值为600
* * *
# ## SET_VOLHIGHT
```text
  void SET_VOLHIGHT(String dev,int channel,int val)
```

```text

```

功能：控制高音部分
**Parameters:**
`dev` \- :音量设备
`channel` \- :设备通道号
`val` \- ：模拟量（注：根据具体外接设备取值。）
示例：
vol_N = N:9:VOL; //定义CRNET设备号为9的音量设备vol_N
SET_ VOLHIGHT (vol_N,1,600);//设置vol_N设备第一路高音部分音量值为600
* * *
# ## SET_VOLLOW
```text
  void SET_VOLLOW(String dev,int channel,int val)
```

```text

```

功能：控制低音部分
**Parameters:**
`dev` \- :音量设备
`channel` \- :设备通道号
`val` \- ：模拟量（注：根据具体外接设备取值。）
示例：
vol_N = N:9:VOL; //定义CRNET设备号为9的音量设备vol_N
SET_VOLLOW (vol_N,1,600);//设置vol_N设备第一路低音部分音量值为600
* * *
**Z_SET_VOLHIGHT**
void**Z_SET_VOLHIGHT**(String dev, char track, int val)
* 功能：声音控制器高音设置。
```text
        * param dev :设备名
```
```text
	*param track:声道。‘L’左声道，‘R’右声道，‘D’双声道  

```
```text
	*param val:模拟值。0-65535
```
```text

```
```text
 示例： 

```
```text
VOL_Z = Z:10:VOL:"0102030405"; //定义ZigBee卡ID为10的音量控制器	
         	         	Z_SET_VOLHIGHT(VOL_Z, 'L', 1000);//设置音量控制器左声道高音值
```
```text

  
                   
Z_SET_VOLLOW
	void Z_SET_VOLLOW(string dev, char track,  int val)
    		* 功能：声音控制器低音音设置。        * param dev :设备名	*param track:声道。‘L’左声道，‘R’右声道，‘D’双声道	*param val:模拟值。0-65535   示例： 
VOL_Z = Z:10:VOL:"0102030405"; //定义ZigBee卡ID为10的音量控制器	
         	         	VOL_SET_VOLLOW(VOL_Z,‘R’, 10000); //设置音量控制器右声道低音值	
```
```text
   Z_SET_VOTOTAL  	void Z_SET_VOLTOTAL(String dev, char track, int val)	* 功能：声音控制器总音量设置。        * param dev :设备名	*param track:声道。‘L’左声道，‘R’右声道，‘D’双声道	*param val:模拟值。0-65535   示例： 
	
         	         	   VOL_Z = Z:10:VOL:"0102030405"; //定义ZigBee卡ID为10的音量控制器	
VOL_SET_VOLTOTAL(VOL_Z, ‘D’,  10000); //设置音量控制器左右两个声道的总音量为1000         	         	  	   Z_SET_VOLQUIET  	void Z_SET_VOLQUIET  (String dev, char track)		* 功能：声音控制器静音设置。按一下静音，再次按下恢复。        * param dev :设备名	*param track:声道。‘L’左声道，‘R’右声道，‘D’双声道   示例： 
		
VOL_Z = Z:10:VOL:"0102030405"; //定义ZigBee卡ID为10的音量控制器		
  Z_SET_VOLQUIET(VOL_Z, ‘L’);         	    
	
```

* * *
Copyright (c) 2008-2012 Creator
<http://www.creator.com.cn>
# ## 